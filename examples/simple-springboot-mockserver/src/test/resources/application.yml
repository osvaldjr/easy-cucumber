# overwrite spring properties

server.port: 9000

feign:
  okhttp:
    enabled: true
    disableSslValidation: true

fakeintegration.url: http://localhost:9005/mock_alias

# easy cucumber properties

target.url: http://localhost:9000

dependencies:
  mockserver.port: 9005